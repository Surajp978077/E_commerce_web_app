@{
    ViewData["Title"] = "List of Actors";
}
@model IEnumerable<Actor>

<div class="row">
    <div class="col-md-8 offset-md-2">
        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>
                        <label>Profile Picture</label>
                    </th>
                    <th>
                        <label>Full Name</label>
                    </th>
                    <th>
                        <label>Email</label>
                    </th>
                    <th>
                        <label>      </label>
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var actor in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="rounded-circle" src="@actor.ProfilePictureURL" alt="@actor.FullName"
                                style="max-width: 150px;">
                        </td>
                        <td class="align-middle">
                            @actor.FullName
                        </td>
                        <td class="align-middle">
                            @actor.Email
                        </td>
                        @* @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                        { *@
                            <td class="align-middle">
                                <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@actor.Id"><i
                                        class="bi bi-pencil-square"></i>Edit </a> |
                                <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@actor.Id"><i
                                        class="bi bi-eye"></i>Details </a> |
                                <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="@actor.Id"><i
                                        class="bi bi-trash"></i>Delete </a>
                            </td>
                        @* } *@
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
